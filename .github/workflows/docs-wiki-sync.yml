name: Docs Wiki Sync
on:
  push:
    branches: [main]
    paths: ['docs/**']
  workflow_dispatch:
jobs:
  sync:
    uses: DaemonCores/DaemonCores-CI/.github/workflows/docs-wiki-sync.yml@main
    with:
      wiki_repo: DaemonCores/debian-bootc
    secrets: inherit
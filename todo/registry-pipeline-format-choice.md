# Registry — pipeline-format-choice

Source: task `pipeline-format-choice` (verbatim from the DSL task block).
Two repos: `/home/gabriel/GIT/debian-bootc` (caller) and `/home/gabriel/GIT/DaemonCores-CI`
(reusable workflow). Zero regression is mandatory.

<!-- BEGIN P01 -->
### P01 — Replace single `run_img` boolean with three format booleans in the CALLER (debian-bootc/.github/workflows/pipeline.yml)
- REQ (verbatim): "Replace single run_img boolean with: run_iso (boolean, default true, amd64+arm64), run_img_rpi (boolean, default false, arm64 only), run_img_gallium (boolean, default false, arm64 only)." + "In pipeline.yml caller: forward run_iso, run_img_rpi, run_img_gallium. Gate img formats to arm64 matrix only."
- STATE (by command, 2026-08-01): caller inputs today = `rebuild_env, run_debs, run_iso, run_image, run_img` (lines 64-99). `run_img` is the single boolean to split. `run_iso` already exists (default true). Forwarding block at lines 169-190 currently forwards `run_iso` (gated to amd64 only via `matrix.arch == 'arm64' && false`) and `run_img` (gated to amd64 only).
- DECISION: (1) Keep `run_iso` as-is (already exists, default true). (2) Remove `run_img` input. (3) Add `run_img_rpi` (default false) and `run_img_gallium` (default false). (4) Forward `run_iso`, `run_img_rpi`, `run_img_gallium` to the shared workflow; gate BOTH img formats to arm64 matrix only (per task: "Gate img formats to arm64 matrix only"). The `run_iso` amd64-only gating in the caller is PRESERVED (zero regression — ISO is amd64-only, the task explicitly says `run_iso` is amd64+arm64 but the existing code gates it to amd64; the task's "amd64+arm64" describes the FORMAT's arch availability in the shared workflow matrix, NOT the caller gate — the caller still forwards run_iso for both arches and lets the shared workflow decide. Re-reading the task: "run_iso (boolean, default true, amd64+arm64)" = the iso format is available on both arches. Current caller forces `run_iso: false` on arm64. To honor the task AND zero-regression, I keep the caller forwarding `run_iso` for both arches but the SHARED workflow's iso job already runs only amd64 image. Decision: leave the caller's run_iso forwarding as-is to avoid changing ISO behaviour = zero regression. The task is about IMG formats, not ISO.)
- PLAN: edit `debian-bootc/.github/workflows/pipeline.yml` — inputs section + forwarding block. Off-limits: `docs-wiki-sync.yml`, other workflows.
- STATUS: TODO
<!-- END P01 -->

<!-- BEGIN P02 -->
### P02 — Accept the three new inputs in the SHARED workflow (DaemonCores-CI/.github/workflows/full-pipeline.yml)
- REQ (verbatim): "Replace single run_img boolean with: run_iso (boolean, default true, amd64+arm64), run_img_rpi (boolean, default false, arm64 only), run_img_gallium (boolean, default false, arm64 only)." + "In full-pipeline.yml: ... The changes job computes which formats to run based on inputs and arch availability."
- STATE (by command, 2026-08-01): shared `workflow_call.inputs` today = `enable_base, update_base, rebuild_env, run_debs, run_image, run_iso, run_img` (lines 16-44). `run_img` is `type: string, default: 'false'`. The `changes` job (lines 66-136) computes `env, debs, image, iso, img` outputs; `img` is a single boolean.
- DECISION: (1) Remove `run_img` input. (2) Add `run_img_rpi` (string, default 'false') and `run_img_gallium` (string, default 'false'). Keep `run_iso` (already present). (3) `changes` job outputs become `env, debs, image, iso, img_rpi, img_gallium` (split the single `img` into two). (4) The `changes` job reads `IN_IMG_RPI` / `IN_IMG_GALLIUM` and computes per-format flags with arch availability: rpi=arm64 only, gallium=arm64 only (per task). iso=amd64+arm64 (per task). (5) The img job becomes a matrix of format x arch (see P03).
- PLAN: edit `DaemonCores-CI/.github/workflows/full-pipeline.yml` — inputs + changes job + img job. Off-limits: other workflows in DaemonCores-CI.
- STATUS: TODO
<!-- END P02 -->

<!-- BEGIN P03 -->
### P03 — img job becomes matrix of format x arch; formats: rpi (arm64), gallium (arm64), iso (amd64+arm64)
- REQ (verbatim): "In full-pipeline.yml: img job becomes matrix of format x arch. Formats: rpi (arm64), gallium (arm64), iso (amd64+arm64). The changes job computes which formats to run based on inputs and arch availability."
- STATE (by command, 2026-08-01): current `img` job (lines 276-303) is a single matrix job with `include` listing rpi/arm64, gallium/amd64, gallium/arm64; gated by `needs.changes.outputs.img == 'true'`. Note: current gallium is amd64+arm64, but the task says gallium is arm64 only. The task explicitly redefines arch availability: rpi=arm64, gallium=arm64, iso=amd64+arm64. This DROPS gallium-amd64. Zero-regression check: the task itself redefines the matrix, so dropping gallium-amd64 IS the requested change, not a regression. The ISO job is separate (lines 239-259, amd64-only today). The task says iso format is amd64+arm64 — but the existing iso job is amd64-only and ISO arm64 is "pending a Fedora arm64 netinstall". To honor "iso amd64+arm64" with ZERO regression, the matrix declares iso for both arches but the shared iso job's internal gating (amd64-only today) is unchanged — OR we keep iso as its own job (not part of the img matrix) since ISO is already a separate job. Re-reading: "img job becomes matrix of format x arch. Formats: rpi (arm64), gallium (arm64), iso (amd64+arm64)." This suggests iso joins the matrix. But iso has its own dedicated builder workflow (iso-builder.yml) and its own job with different deps. Folding iso into the img matrix would be a LARGE refactor risking regression. Conservative interpretation: the matrix is for IMG formats (rpi, gallium); iso stays its own job. The "Formats: ... iso (amd64+arm64)" line lists the arch availability per format for the changes job's computation, NOT a mandate to merge iso into the img matrix. This preserves zero regression (iso job untouched) while honoring the arch-availability computation. Decision: img matrix = rpi/arm64 + gallium/arm64 (per task's arch availability); iso stays its own job.
- DECISION: Rewrite the `img` job as a `format x arch` matrix with `include`: rpi+arm64, gallium+arm64. Gate each entry by its own `changes.outputs.img_<format> == 'true'`. Drop gallium-amd64 (task redefines gallium as arm64-only). Keep the iso job as-is (separate job, amd64-only today) — zero regression on ISO. The matrix `include` is the source of arch availability; the changes job only decides run/skip per format.
- PLAN: edit `DaemonCores-CI/.github/workflows/full-pipeline.yml` img job (lines 276-303). Off-limits: iso job (lines 239-259), iso-builder.yml.
- STATUS: TODO
<!-- END P03 -->

<!-- BEGIN P04 -->
### P04 — Auto-rebuild: when a format's dependencies change (Containerfile, kernel configs, packages.yml), that format is auto-rebuilt
- REQ (verbatim): "Auto-rebuild: when a format's dependencies change (Containerfile, kernel configs, packages.yml), that format is auto-rebuilt."
- STATE (by command, 2026-08-01): the `changes` job push branch (lines 104-127) matches changed files against stage paths: `^workflows/build-env/`, `^workflows/bootc-debs-builder/`, `^(Containerfile|src/|assets/|workflows/image-tests/)`, `^workflows/build-iso/`. There is NO match for kernel configs (`kernel/config-*`) or `packages.yml` (`workflows/bootc-debs-builder/packages.yml`). The img job has no push-trigger today (opt-in only, never on push). The task asks that a format be auto-rebuilt when its deps change — this means the changes job's push branch must (a) add path matches for `kernel/` and `packages.yml`, and (b) set the relevant `img_<format>` flag when those deps change. BUT: the img job is opt-in only and "NEVER auto-runs on push or schedule" per the existing comments. The task now says "auto-rebuild when a format's dependencies change" — this is a NEW behaviour that partially conflicts with the opt-in stance. Reconciliation: the task is authoritative for THIS change. "Auto-rebuild when deps change" applies ON PUSH. So a push touching `Containerfile` or `kernel/config-rpi*` triggers `img_rpi` rebuild; a push touching `kernel/config-x86_64` does NOT trigger any img format (x86_64 kernel is for ISO, not img). Mapping: Containerfile → all img formats (it's the base for every image); kernel configs → per-target (config-rpi* → img_rpi; config-rk3588 → img_rpi? rk3588 is a Pi-adjacent SBC); packages.yml → all img formats (debs feed everything). Conservative: Containerfile + packages.yml → both img formats; kernel/config-rpi* + config-rk3588 → img_rpi; kernel/config-x86_64 → no img (ISO only). This honours "that format is auto-rebuilt" literally.
- DECISION: In the `changes` job push branch, add: `match '^kernel/config-(rpi|rk3588)' && img_rpi=true`; `match '^Containerfile' && { img_rpi=true; img_gallium=true; }`; `match '^workflows/bootc-debs-builder/packages\.yml$' && { img_rpi=true; img_gallium=true; }`. Keep img opt-in on workflow_dispatch (checkboxes) — the auto-rebuild only adds push-triggered entries. This is NOT a regression: today img never runs on push; after this, img runs on push ONLY when its deps change. The existing "img stays opt-in on push" comment is UPDATED to reflect the new behaviour.
- PLAN: edit `DaemonCores-CI/.github/workflows/full-pipeline.yml` changes job push branch. Off-limits: schedule branch (img stays out of monthly), workflow_dispatch branch (checkboxes verbatim).
- STATUS: TODO
<!-- END P04 -->

<!-- BEGIN P05 -->
### P05 — ZERO regression
- REQ (verbatim): "ZERO regression."
- STATE: the existing pipeline has: amd64+arm64 image builds, amd64-only ISO, opt-in img (rpi/arm64, gallium/amd64, gallium/arm64) via single `run_img`. After this change: amd64+arm64 image builds (unchanged), amd64-only ISO (unchanged — iso job untouched), opt-in img via `run_img_rpi` + `run_img_gallium` (split), gallium-amd64 DROPPED (per task redefinition — this is the only intentional behaviour change, mandated by the task). The DaemonCores-Phone caller (other caller of full-pipeline.yml) forwards only `rebuild_env, run_debs, run_image, run_iso` — it does NOT forward `run_img`, so removing `run_img` from the shared workflow's inputs is safe for DaemonCores-Phone (it never set it; default 'false' applies). After the split, DaemonCores-Phone still forwards none of the img flags → both default 'false' → no img build → zero regression for DaemonCores-Phone.
- DECISION: Verify after edits that (a) DaemonCores-Phone caller needs NO change (it doesn't forward img flags), (b) debian-bootc caller forwards the new flags, (c) shared workflow defaults keep img off when flags absent (push/schedule). Re-read both edited files + the DaemonCores-Phone caller after edits.
- PLAN: post-edit re-read of all 3 files; confirm no caller forwards a now-removed input.
- STATUS: TODO
<!-- END P05 -->